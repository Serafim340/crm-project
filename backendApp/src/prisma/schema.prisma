generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Location {
  number          String @id @default(uuid())
  location    String @unique
  name        String
  description String
  text        String
}
